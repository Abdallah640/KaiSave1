function deposit() {
  const amt = document.getElementById('depositAmount').value;
  alert('Deposited ' + amt + ' USDT into vault (demo only)');
}

function withdraw() {
  const amt = document.getElementById('withdrawAmount').value;
  alert('Withdrew ' + amt + ' USDT from vault (demo only)');
}

function joinCircle() {
  alert('You joined the KaiCircle (demo only)');
}

function contribute() {
  alert('You contributed to KaiCircle (demo only)');
}

function payout() {
  alert('KaiCircle payout executed (demo only)');
}
